import 'package:flutter/material.dart';

class EventsActivity extends StatelessWidget {
  const EventsActivity({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.orange,
      body: const Center(
          child: Text(
        "event pagese",
        textScaleFactor: 2,
      )),
    );
  }
}
